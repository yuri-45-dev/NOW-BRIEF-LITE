<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runtime permission (Android 13+) to post any notification at all -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 16 (API 36): required to request promotion of an ongoing
         notification into a Live Update (status-bar chip / lock-screen capsule). -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />

    <!-- We only ever schedule 4 alarms/day at fixed clock boundaries, never a
         repeating short-period alarm, so exact alarms are used sparingly and
         within Android's "alarm clock app" battery exemptions. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Re-arm the 4 daily boundary alarms after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.NowBrief"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NowBrief">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fired only by our own AlarmManager PendingIntents — never exported,
             never polling, never a background service. -->
        <receiver
            android:name=".PeriodAlarmReceiver"
            android:exported="false" />

        <!-- Re-schedules the 4 daily boundary alarms once, right after boot,
             then goes back to sleep until the next boundary. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Home-screen "Now Brief" capsule widget (pill layout). Updated only
             from PeriodAlarmReceiver — updatePeriodMillis is intentionally
             left unset so the OS never polls it on its own. -->
        <receiver
            android:name=".widget.NowBriefWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/now_brief_widget_info" />
        </receiver>

    </application>
</manifest>
